# Local development only. Docker exposes this service exclusively on 127.0.0.1.
# Production will use MQTT over TLS with per-device certificates and ACLs.
persistence true
persistence_location /mosquitto/data/
log_dest file /mosquitto/log/mosquitto.log
listener 1883
allow_anonymous true
